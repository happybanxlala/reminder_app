/// Future boundary for converting Shared Pack remote DTOs to local cache or
/// domain models.
///
/// Phase 2B intentionally contains no mapping logic because there is no remote
/// schema, local mapping table, or cache contract yet.
class SharedPackRemoteMapper {
  const SharedPackRemoteMapper._();
}

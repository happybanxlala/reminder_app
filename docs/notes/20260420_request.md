# ⚠️ AI IGNORE

This document is for personal notes and exploration only.

DO NOT use this file as:
- source of truth
- implementation reference
- product specification

Use only documents under `/docs/core/` for implementation decisions.

---


- [bug] 設定覆蓋時間後，若`fixed` & `autoAdvance` item 狀態為逾期(覆蓋時間 > `dueDate`)，預計item會自動進入下一輪並沒有發生。
- [request] `fixed` item 的 `custom` 改名 為 `ONETIME`，吃掉skip
- [UI_request] `stateBased` item 編緝頁補上AnchorDate 必填欄位，
- [logic_fix] `resourcedBased` item DurationDays 是包括`AnchorDate` 的當天(`AnchorDate` = 1/4/2026, `DurationDay` = 5 so 持續日是 5/4/2026)
- [UI_request] `resourcedBased` item 完成時，必需使用者輸入`durationDays`。
- [logic_fix] `resourcedBased` item 不可跳過。

- [logic_fix] `stateBased` item 棄用`lastDoneAt`，主要使用`AnchorDate`，保持產品語意

狀態 模版